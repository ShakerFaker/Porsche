const Products = () => {
  return <div>Products</div>;
  //SARAH ALAA NAGAR

  //code to get all products

  //isGuest && <button for buy redirect to login>
  //isAdmin && <button for add product and delete product>
  //isLogged && <button for buy product>
};

export default Products;
